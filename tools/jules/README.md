# jules

Purpose:
- GitHub-oriented maintenance and docs support.

Use when:
- the task centers on issue/PR style maintenance.

Do not use when:
- you need to build a new product feature from scratch.

Safe usage rules:
- Preserve existing repository conventions.
- Work through branches rather than main.
- Document follow-up fixes clearly.

Related docs:
- `docs/jules.md`
- `docs/resources/`
