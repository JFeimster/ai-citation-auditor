# Vercel

Purpose:
- Static deployment and preview validation.

Use when:
- You need deploy checks or preview verification.

Do not use when:
- The task is only local content editing.

Safe usage rules:
- Confirm public URLs and asset paths.
- Check sitemap and robots after changes.
- Avoid deployment assumptions.

Related docs:
- `docs/vercel.md`
- `docs/resources/`
