# Prompts

Repo-safe prompts:
- Review a deployment preview for missing assets, layout breaks, or metadata mistakes.
- Check whether a static site change is safe to push to production.

QA prompts:
- Confirm the canonical URL, OG metadata, and asset paths on the deployed page.
- Confirm `sitemap.xml` and `robots.txt` match the production site.
